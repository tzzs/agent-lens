Placeholder so the directory exists in a checkout: it holds NO rollout files, which is
codex.md §4.4-row-4-shaped coverage loss (a sessions directory that survived upstream
retention with nothing in it). `detect()` must report present-with-no-files, not absent.
